using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ListOrders : MonoBehaviour
{
    // Start is called before the first frame update
    public Queue<Order> orders;
    void Start()
    {
       
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
